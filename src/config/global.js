// * 全局不动配置项 只做导出不做修改

// * 首页地址（默认）
export const HOME_URL = '/home'

// * 登录页地址（默认）
export const LOGIN_URL = '/login'

// * token过期状态码
export const TOKEN_STATE = ['A02309999', 'A02319999']

// * Tabs（黑名单地址，不需要添加到 tabs 的路由地址）
export const TABS_BLACK_LIST = ['/404', '/layout']
