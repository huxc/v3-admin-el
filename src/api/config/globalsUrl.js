import { domain_list } from '@/api/config/domainConfig'

// 文件上传
export const img_upload_url = `${domain_list.foundation}/template/update`
export const file_upload_url = `${domain_list.user}/upload/file`
